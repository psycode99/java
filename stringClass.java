public class stringClass {
    public static void main(String[] args) {
        String name = "Favour";

      //boolean result = name.equals("favour");
      //boolean result = name.equalsIgnoreCase("favour");
      //int result = name.length();
      //char result = name.charAt(5);
      //int result = name.indexOf("v");
        //boolean result = name.isBlank();
        //String result = name.toLowerCase();
        //String result = name.toUpperCase();
        //String result = name.replace("a", "i");
        boolean result = name.startsWith("f");

        System.out.println(result);
    }
}
